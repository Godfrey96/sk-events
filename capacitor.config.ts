import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pkmt.skeventApp',
  appName: 'SK-Event App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
